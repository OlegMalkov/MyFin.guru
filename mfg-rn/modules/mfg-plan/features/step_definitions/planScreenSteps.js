/* eslint-disable */
/* @flow */

import { myDefineSupportCode } from 'mfg-base/features/utils/myDefineSupportCode'
import { PlanScreenTestWorld } from '../PlanScreenTestWorld'

const World: PlanScreenTestWorld = (PlanScreenTestWorld: any)

myDefineSupportCode(
  World,
  ({ Given, When, Then }) => {

  })
